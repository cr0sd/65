#define JOYP	0x6000
#define RAM(x)	((char*)0)[x]

void drawstuff(char*);

void main(void)
{
	drawstuff("hello there");
	while(1);
}

void drawstuff(char*str)
{
	int i=0;
	for(i=0;str[i];++i)
		RAM(JOYP)=str[i];
}

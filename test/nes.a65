.byt	"NES",0
.word	0,0,0,0,0,0

.text
*=$8000
main:

	; INDIRECT INDEXED
	;lda ($00),y
	;lda( fetch16( zp( $LL ) + Y ) )

	lda #$99
	sta $0100

	lda #$01
	sta $01

	ldy #$00
	lda ($00),y

	; INDEXED INDIRECT
	;lda ($00,x)
	;lda( fetch16( zp( $LL + X ) ) )

	jmp main

.byt "string",0
